---
description: このプロジェクトのユーザーが好むコードスタイルの実例集。コードを書く・磨くときに自動適用される。
paths:
  - "**/*.py"
---

## コードの好み(ユーザーの実例から蓄積)

ユーザーがコードレビューで「読みやすい」「気持ち悪い」と感じた実例を蓄積する。
コードを書くとき・磨くときは、ここの実例の方向性に合わせる。
一般原則のルールと矛盾する場合、**純粋なスタイル(命名・整形・構造の好み)に
限り**このファイルを優先する(型ヒント・最小diff・秘密情報の扱い等、
安全性・品質に関わる規約は上書きしない)。
運用の詳細(実例の追記はユーザーの発言を契機に提案する等)は README 3.13 節。

### 好む書き方
(まだ実例なし)

### 避けたい書き方
(まだ実例なし)
