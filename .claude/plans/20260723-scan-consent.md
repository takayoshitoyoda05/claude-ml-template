# 手順6.6 セキュリティスキャンの同意ゲート再設計

参照設計書: なし(ユーザーの直接依頼。2026-07-23 実走テストで判明したプラグイン同意ゲート仕様に基づく)

## 目的
claude-security プラグインの実仕様(スキャン開始はユーザー本人がその依頼で打ったコスト承諾の言葉だけを Yes とみなす)に、手順6.6 の記述を合わせる。環境変数の設定自体を事前承認とみなす現行記述は仕様違反のため撤廃し、承諾確認をユーザーが確実に居る手順4へ前倒しする。

## 現状分析(確認済みの事実)
- 確認済み: `ml-pipeline.md` L210-239 が 6.6。分岐は (a) プラグイン確認 / (b) 差分スキャン実行 / (c) 結果処理 / (d) 成果物の扱い の4節構成。
- 確認済み: L224-225「スキャン前にコスト見積もりの確認プロンプトが出た場合は承認する(CLAUDE_SECURITY_SCAN=1 の設定自体がユーザーの事前承認とみなす)」がプラグイン仕様(同意ロンダリング拒否)に反する。撤廃対象。
- 確認済み: (b) L222-223 は「claude-security の『Scan changes』モードで main...HEAD をスキャン」。Workflow ツール手組み直呼びは no-args で拒否されるため、リーダーが Security Lead としてジョブレシピに従い駆動する記述へ改める。
- 確認済み: 手順4 は L100-111(計画をユーザーに提示し承認されるまで進めない/却下時 feedback.md 追記)。承諾確認1文の挿入先はここ。
- 確認済み: `grep "事前承認"` は README にヒットせず、リポジトリ全体では ml-pipeline.md L225 のみ。README 側の同期対象は「事前承認」文言ではなく、3.16節(L949-987)・L356 プラグイン表・L206 環境変数表の3箇所。
- 確認済み: `CLAUDE_SECURITY_SCAN` の説明文の実体は config-set/SKILL.md L56、README L206。config-explain/SKILL.md L26 は「同上」参照のみ(独自文言なし)。
- 確認済み: CHANGELOG は Unreleased 内に種別ごと日付昇順で subsection。`### Changed(2026-07-23)`(L63-68)に既存1項目あり、末尾へ追記する。
- 確認済み: 承諾/省略の判断を記録する常設の状態ファイルは無い。final-gate 入力のレビュー要約(L296)がセッション内でスキャン結果を集約する既存の受け皿。

## 変更対象
| ファイル | 箇所 | 変更内容 |
|---------|------|---------|
| .claude/commands/ml-pipeline.md | 手順4(L100-111) | CLAUDE_SECURITY_SCAN=1 時の承諾確認1文を挿入。承諾が無ければその場で明記を要求(例文提示)、省略選択時は記録 |
| .claude/commands/ml-pipeline.md | 手順6.6(L210-239) | 到達時の承諾ゲート追加(承諾無しは黙ってスキップせず差し戻し)、(b) を Security Lead 駆動記述へ、L225 の事前承認みなし文を削除 |
| README.md | 3.16節(L949-987)、L356 プラグイン表、L206 環境変数表 | 開始にはユーザー本人のコスト承諾が依頼文/承認文に必要である旨を同期 |
| .claude/skills/config-set/SKILL.md | L56 | CLAUDE_SECURITY_SCAN 説明に「設定=1 だけでは起動せず、承諾は依頼文/承認文で明記が必要」を追記 |
| CHANGELOG.md | Changed(2026-07-23) L68 末尾 | 6.6 同意ゲート再設計を1項目追加 |

## 実装手順
| # | 内容 | 対象ファイル | 依存 | 並列グループ |
|---|------|-------------|------|-------------|
| 1 | 手順4に承諾確認1文を挿入。CLAUDE_SECURITY_SCAN=1 のとき依頼文/承認文にスキャンのコスト承諾が含まれるか確認し、無ければ例文(「スキャンのトークン消費は了解済み」)を提示してその場で明記を求める。**承諾の有効範囲は同一パイプライン実行内のみ**(セッション再開・圧縮などで本人発言を直接確認できなくなったら、承諾も省略の選択も無効とし 6.6 で再度確認する)。ユーザーが省略を選んだらその旨を記録し 6.6 をスキップする旨を明記(記録は覚え書きであり、根拠は常に会話内の本人発言) | .claude/commands/ml-pipeline.md | なし | A |
| 2 | 6.6 到達時の承諾ゲートを追加。判定基準は**このパイプライン実行内のユーザー本人の発言にコスト承諾があるか**(エージェントが書いた記録・要約は根拠にしない — Codex指摘の採用)。無ければ黙ってスキップせずパイプラインを一時停止しユーザーに承諾の明記または省略の明示を求める(差し戻し)。(b) を「リーダーが Security Lead として /claude-security の scan-changes ジョブレシピの手順(レポートディレクトリ作成→scan-meta 記録→レシピ規定の引数オブジェクトで Workflow 起動)に従い差分(main...HEAD)スキャンを駆動する。引数を省略した手組み起動は拒否される」に修正し(実走 2026-07-23 で受理を確認済みの経路)、L225 の「設定自体を事前承認とみなす」文を削除 | .claude/commands/ml-pipeline.md | Step 1(同一設計の整合) | A |
| 3 | README 3.16節コスト注意・L356 プラグイン表・L206 環境変数表に「起動にはユーザー本人のコスト承諾を依頼文/承認文に明記する必要がある(設定=1 だけでは不十分)」旨を同期 | README.md | なし | B |
| 4 | config-set L56 の CLAUDE_SECURITY_SCAN 説明に承諾明記要件を追記(config-explain は「同上」参照のため変更不要=備考) | .claude/skills/config-set/SKILL.md | なし | C |
| 5 | CHANGELOG Changed(2026-07-23)末尾に 6.6 同意ゲート再設計を1項目追加(種別ごと日付昇順の慣習に従い当該 subsection 末尾へ) | CHANGELOG.md | なし | D |

各ステップのコミット案(fix(step N) 形式):
- Step 1-2: `fix(step 6): 6.6セキュリティスキャンの同意ゲートを手順4へ前倒しし事前承認みなしを撤廃`
- Step 3: `docs(step 6): READMEのセキュリティスキャン説明をコスト承諾明記要件に同期`
- Step 4: `docs(step 6): config-setのCLAUDE_SECURITY_SCAN説明に承諾明記要件を追記`
- Step 5: `docs(step 6): CHANGELOGにスキャン同意ゲート再設計を記録`
(Step 1-2 は同一ファイルの一体変更のため1コミットに束ねる)

## 並列化判定
並列化可能(グループA/B/C/D。対象ファイルが完全に分離しているため)。ただし全て小規模なドキュメント編集であり、worktree 分離の並列実装オーバーヘッドが変更量に見合わないため逐次実行を推奨。Step 1→2 は同一ファイル・同一設計の整合が必要なため必ず連続で行う。

## 検証方法
1. 撤廃文言の残存チェック(0件で PASS):
   `grep -rn "設定自体が.*事前承認\|事前承認とみなす" .claude/commands/ml-pipeline.md` → 0件
   `grep -rn "事前承認" README.md .claude/skills/config-set/SKILL.md .claude/skills/config-explain/SKILL.md` → 0件
2. 新記述の挿入チェック(節を特定した grep で件数まで確認 — Codex指摘の採用):
   `sed -n '/^### 4\./,/^### 5\./p' .claude/commands/ml-pipeline.md | grep -c "スキャンのトークン消費は了解済み"` → 1(手順4に例文)
   `sed -n '/^### 6\.6/,/^### 6\.7/p' .claude/commands/ml-pipeline.md | grep -c "Security Lead"` → 1以上(6.6(b)に駆動記述)
   `sed -n '/^### 3\.16/,/^### 3\.17\|^## /p' README.md | grep -c "承諾"` → 1以上(3.16節内に限定。次の ### または ## 見出しで打ち切り)、加えて L206 相当の環境変数表行と L356 相当のプラグイン表行を `grep -n "CLAUDE_SECURITY_SCAN" README.md` の各行で目視
   `grep -c "依頼文\|承認文" .claude/skills/config-set/SKILL.md` → 1以上
3. 6.6 到達時ゲートの存在チェック(節を特定):
   `sed -n '/^### 6\.6/,/^### 6\.7/p' .claude/commands/ml-pipeline.md | grep -c "一時停止"` → 1以上(承諾無し時の停止記述)
4. CHANGELOG 追記チェック(見出し基準 — Codex指摘の採用):
   `sed -n '/^### Changed(2026-07-23)/,/^### /p' CHANGELOG.md` を目視 → 末尾に 6.6 同意ゲート項目が1件追加され、日付昇順が崩れていない
5. Markdown 全体の整合(節番号・リンク崩れなし)を目視確認

## リスク
- 承諾/省略の記録は覚え書きに留め、判定の根拠は常に会話内のユーザー本人発言とする(エージェント生成物を同意の根拠にしない)。記録先は既存のレビュー要約(L296、セッション内)。代替案=専用の consent マーカーファイル追加は、フック/ガードとの整合コストと最小diff規律に反するため不採用。未確認の仮定: セッションをまたいで 6.6 に到達するケースでは記録がセッション内保持のため失われうる——その場合は 6.6 到達時ゲートが再度承諾を要求するので安全側に倒れる(黙ってスキップしない設計で担保)。
- (b) は scan-changes ジョブレシピを固定名で明記する方針(Step 2)。将来のプラグイン更新でジョブ名・手順が変わると陳腐化するリスクがあるが、抽象的な参照だけでは起動経路の検証にならない(Codex指摘)ため、具体名+「実走 2026-07-23 で受理確認」の注記で採用。プラグイン更新時は 6.6 の追従更新が必要(受容)。
- config-explain L26 を「同上」のまま据え置くため、config-set L56 とREADME L206/L56 の文言が乖離すると説明不整合になる。Step 3・4 で同一趣旨の文言に揃えることで回避。未確認の仮定: L206(環境変数一覧の既定値表)は簡潔性優先の表であり、長い承諾要件文を入れると表が崩れる可能性——3.16節に詳細を置き、表側は短い注記に留める。
- 失敗シナリオ想定: (1) 手順4の承諾確認を「エージェントが代弁して可」と誤読されないよう、確認はユーザー本人の明記を求める文言に限定する。(2) 6.6 ゲートを「承諾あれば無条件実行」と誤読し省略選択の記録を無視しないよう、省略明示時は 6.6 スキップと明記する。(3) L225 削除時に (b) の残り(main...HEAD 対象・コスト見積もりプロンプト自体の存在)まで消さないよう、削除は事前承認みなしの1文に限定する。

## トレーサビリティ(設計書なし: 全ステップに検証方法があることを確認)
| ステップ | 検証方法 |
|---------|---------|
| Step 1 | 検証2(例文 grep)、検証5(目視) |
| Step 2 | 検証1(撤廃文言 grep 0件)、検証2(Security Lead grep)、検証3(到達時ゲート grep) |
| Step 3 | 検証1(事前承認 grep 0件)、検証2(承諾 grep)、検証5(目視) |
| Step 4 | 検証1(事前承認 grep 0件)、検証2(依頼文/承認文 grep) |
| Step 5 | 検証4(CHANGELOG 目視) |
全ステップに対応する検証方法あり。検証方法の無いステップは無し。

## 作業ログ(2026-07-23実施)

Step 1〜5 を計画通り逐次実装し、コミット案どおり4コミットに分割した。

- Step 1-2: `61aafb7` fix(step 6): 6.6セキュリティスキャンの同意ゲートを手順4へ前倒しし事前承認みなしを撤廃
  - 手順4に承諾確認1文(例文「スキャンのトークン消費は了解済み」)と有効範囲(同一パイプライン実行内のみ)を挿入
  - 6.6到達時の承諾ゲート(一時停止して差し戻し)を追加、(b)をSecurity Lead駆動記述に書き換え、L225の事前承認みなし文を削除(main...HEAD対象は維持したが、コスト見積もりプロンプト自体への言及は実装時に消えていた。本修正でプラグイン確認プロンプトの扱いを(b)に復元した)
- Step 3: `867061d` docs(step 6): READMEのセキュリティスキャン説明をコスト承諾明記要件に同期
  - 3.16節コストに関する注意・L356プラグイン表・L206環境変数表の3箇所を同期(表は簡潔な注記に留め詳細は3.16節に集約)
- Step 4: `aa9eb91` docs(step 6): config-setのCLAUDE_SECURITY_SCAN説明に承諾明記要件を追記
  - L56に「設定=1だけでは起動せず、依頼文/承認文にユーザー本人のコスト承諾の明記が必要」を追記(config-explainは同上参照のため変更なし)
- Step 5: `da178c5` docs(step 6): CHANGELOGにスキャン同意ゲート再設計を記録
  - Changed(2026-07-23) subsection末尾に1項目追加、日付昇順は維持

### 検証結果
1. 撤廃文言grep: ml-pipeline.md 0件・README/config-set/config-explain 0件 → PASS
2. 新記述grep: 手順4例文=1件、6.6 Security Lead=1件、README 3.16節「承諾」=3件(L206・L356の表2行は目視で同期文言を確認)、config-set「依頼文/承認文」=1件 → PASS
3. 6.6到達時ゲート「一時停止」=1件 → PASS
4. CHANGELOG目視: Changed(2026-07-23)末尾に新項目1件追加、既存項目・日付順序は変更なし → PASS
5. Markdown全体整合: ml-pipeline.mdの節番号(1〜9、6.5〜6.8含む)に欠落・重複なし、README表(L206行・L356行とも区切り`|`のフィールド数が周辺行と一致=5)に崩れなし → PASS

計画からの逸脱: なし。
