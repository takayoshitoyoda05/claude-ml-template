.claude/checkpoints/
__pycache__/

# 運用中に溜まるローカルなログ・作業用ドキュメント(テンプレートの配布物ではない)
.claude/improvements/feedback.md
docs/

# agents/shared/ からの生成物と Codex ローカル設定(claude-update が再生成する)
# ルート直下のみ対象(サブディレクトリの階層別 AGENTS.md を誤って無視しないため)
/AGENTS.md
/.codex/

# ガード保護ファイルの変更をユーザーに手動反映してもらうための一時ファイル
# (コミット対象になることは無く、codex_gate の未追跡チェックを塞がないよう除外)
/_staging_*.py

# 並列実装のグループ別worktree(/ml-pipeline 手順5が作成し、完了後に remove する)
/.worktrees/
/.sandbox-worktrees/
